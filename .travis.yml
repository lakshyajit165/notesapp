sudo: required

services:
    - docker


before_install:
    - docker build -t lakshyajit165/notesapp .

script:
    - echo "skipping tests"

after_success:
    - echo "$DOCKER_PASSWORD" | docker login -u "$DOCKER_ID" --password-stdin
    - docker push lakshyajit165/notesapp